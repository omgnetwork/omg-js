{
  "name": "integration-tests",
  "version": "0.0.1",
  "scripts": {
    "mocha": "mocha --timeout=0 --bail --slow=0 --check-leaks --full-trace",
    "deploy-test-erc20": "cd tokens && truffle deploy",
    "audit-check": "audit-ci --moderate",
    "preinstall": "npm-force-resolutions",
    "sequence-test": "npm run mocha sequence-test",
    "ci-min-smoke-test": "npm run mocha ci-min-smoke-test",
    "ci-baseline-test": "npm run mocha ci-baseline-test",
    "ci-ife-test": "npm run mocha ci-ife-test",
    "parallel-test": "node parallel-test",
    "cleanup-faucets": "node cleanup-faucets"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "human-standard-token-abi": "^2.0.0",
    "node-fetch": "^2.6.0",
    "npm-force-resolutions": "0.0.3",
    "promise-retry": "^1.1.1",
    "web3": "1.2.2"
  },
  "devDependencies": {
    "audit-ci": "^2.5.1",
    "mocha": "^6.2.3",
    "mocha-parallel-tests": "^2.2.2",
    "truffle": "^4.1.14"
  },
  "resolutions": {
    "elliptic": "6.5.3"
  }
}
